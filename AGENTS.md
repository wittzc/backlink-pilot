# Backlink Pilot — Agent Instructions (Codex / OpenHands / generic)

This file is a redirect. The single source of truth for AI-agent instructions
in this repo is **[docs/AGENT_GUIDE.md](docs/AGENT_GUIDE.md)**.

Read that file before doing anything in this repo. It covers:

- First-time user flow (prerequisites, config, first submission)
- Available CLI commands and their exit-code contract
- Site adapters and `targets.yaml` schema
- Operating rules (pacing, dedup, verdict layer behavior)
- Error handling table
- Batch submission playbook

For machine-readable command metadata see
[`agent-manifest.json`](agent-manifest.json).
